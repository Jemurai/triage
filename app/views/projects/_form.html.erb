<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>


  <div class="field">
    <%= f.label :priority %><br />
    <%= f.number_field :priority %>
  </div>
  <div class="field">
    <%= f.label :rank %><br />
    <%= f.number_field :rank %>
  </div>
  <div class="field">
    <%= f.label :tier %><br />
    <%= f.number_field :tier %>
  </div>
  <div class="field">
    <%= f.label :verified %><br />
    <%= f.check_box :verified %>
  </div>

  <div class="clearfix">
    <%= f.label :rich_description %><br />
	<%= f.cktext_area :rich_description, :value => "Default value", :id => "rich_text_description" %>
	
	<%# f.text_area :rich_description, :as => :ckeditor, :input_html => { :ckeditor => {:toolbar => 'Full'} } %>
</div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
